{% set name = "avro" %}
{% set version = "1.12.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://files.pythonhosted.org/packages/source/{{ name[0] }}/{{ name }}/avro-{{ version }}.tar.gz
  sha256: e900e7b59a4781f63d9e935e88f6ec50c4d60be3f4adf9c63b51ed39d578fe08

build:
  noarch: python
  entry_points:
    - avro = avro.__main__:main
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=40.8.0
    - wheel
    - pip
  run:
    - python >={{ python_min }}
    # - zstandard 
    # - python-snappy 
    # - zstandard 
    # - python-snappy 

test:
  imports:
    - avro
  commands:
    - pip check
    - avro --help
    #- test -f ${PREFIX}/lib/libavro.a  # [unix]
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://avro.apache.org/
  summary: Avro is a serialization and RPC framework.
  license: Apache-2.0
  license_file:
    - avro/LICENSE
    - avro/NOTICE

extra:
  recipe-maintainers:
    - rxm7706
    - mariusvniekerk
